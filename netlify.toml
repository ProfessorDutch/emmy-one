[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Nitro's `netlify` preset emits the SSR handler as a Netlify Function and
# writes the required _redirects into dist/. No manual redirect rules
# are needed here.
